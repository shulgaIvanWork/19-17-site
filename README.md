# 19×17 - сайт на React

Утвержденный дизайн (`../Meridian Site v3.dc.html`), собранный на Next.js App Router.
**Сайт русскоязычный.**

## Структура

У каждой услуги своя страница. Решение заказчика от 2026-09-22: девять отдельных
страниц вместо двух хабов с якорями, услуги раскрываются панелью под вкладкой
«Главная». Отдельной витрины услуг нет: полный список - блок `#services` на главной,
вкладка ведет туда. Название услуги одно на все места и берется из реестра
(`content/services.ts`).

| Адрес | Что там |
|---|---|
| `/` | Главная, блок `#services` - все девять услуг карточками |
| `/services/landing` | Лендинг |
| `/services/multipage` | Многостраничный сайт |
| `/services/marketplace` | Интернет-магазин |
| `/services/redesign` | Обновление сайта |
| `/services/support` | Поддержка сайта |
| `/services/onec` | Интеграция с 1С |
| `/services/crm` | Интеграция с CRM |
| `/services/vpn` | Корпоративный VPN |
| `/services/ai` | Локальный AI |
| `/works` | Работы |
| `/pricing` | Цены |
| `/about` | О компании |
| `/privacy` | Политика обработки персональных данных |
| `/api/enquiry` | Прием заявки из формы |

Шапка: Главная ▾ · Работы · Цены · О нас. Отдельной вкладки «Услуги» нет: панель с
девятью услугами раскрывается при наведении на «Главную» (`HomeMenu`), четыре колонки
по группам - разработка с нуля, обслуживание сайтов, интеграции, инфраструктура.
Нажатие на саму вкладку ведет на главную, где те же услуги стоят карточками. У названия
вкладки стрелка: пункт «Главная» обычно ничего не раскрывает, без метки панель легко не
заметить. На странице услуги подсвечена «Главная» - других вкладок у услуг нет.

Старые адреса - серверные редиректы в `next.config.ts`: `/vpn`, `/ai`, `/support`,
`/website-redesign`, `/online-store`, `/integrations/crm`, `/integrations/1c` прямо на
свои услуги, `/integrations`, `/maintenance` и `/services` на блок услуг главной.
`/websites` и `/vpn-ai` ведут на `/` БЕЗ якоря намеренно: якорь серверу не приходит,
браузер переносит его сам, и на главной его разбирает `LegacyHashRedirect`. Сейчас это
307, пока структура может меняться; перед запуском перевести на `permanent: true`.

```bash
npm install
npm run dev        # http://localhost:3000
npm run build      # продакшен-сборка
npm run lint       # eslint .
npm run typecheck  # tsc --noEmit
```

Ловушка: после удаления или переименования маршрута `tsc` ругается на старые типы в
`.next/types`. Удалите `.next`, он пересоздается при сборке.

Линтер - ESLint с конфигурацией `eslint-config-next` (`eslint.config.mjs`). Команда
`next lint` в Next 16 удалена, настройка плоская, по документации в
`node_modules/next/dist/docs`. Включены правила Next, React, хуков и typescript-eslint;
последние ловят неиспользуемый код, который `tsc` пропускает.

## Где что лежит

| Папка | Что внутри |
|---|---|
| `app/` | По папке на маршрут. `layout.tsx` - шапка, футер, провайдер формы и декор. `globals.css` - токены и общий словарь классов. `api/enquiry/route.ts` - единственный бэкенд. |
| `components/` | Компоненты по папкам-фичам, рядом с каждым его `*.module.css`. |
| `content/` | Только данные: реестр услуг (`services`), навигация (`nav`), тексты сайта (`products`, `pricing`, `team`, `site`, `legal`) и `settings.ts` с переключателями анимации героя. Обращений к DOM здесь нет. |
| `lib/` | Код, общий для клиента и сервера: `enquiry.ts` - проверка заявки, одна для формы и маршрута API. |

Папки `components/`:

| Папка | Что внутри |
|---|---|
| `ui/` | Базовые элементы без знания о контенте: `Button`, `TextLink`, `Section`, `Overlay`, `Field`, `ChoiceRow`, `Photo`. |
| `blocks/` | Блоки страниц, собранные из данных `content/`: `ProductCard`, `PricingCard`, `CompareTable`, `NumberedClaim`, `TwoColList`, `PlanFigure`, `TeamCarousel`. Полосы страницы услуги - `ClaimRow`, `SceneBand`, `PlanBand`, `CompareBand`, их отступы в `ServiceBand.module.css`. Марки услуг - `CardMark` (плоские) и `PricingMark` (изометрические), общие правила их стилей - в `markBase.module.css`. |
| `contact/` | Форма заявки: `ContactContext` (провайдер), `ContactModal`, кнопки вызова, `Consent`. |
| `nav/` | Шапка, панель услуг под вкладкой «Главная» (`HomeMenu`), мобильное меню и общее поведение меню (`useHoverMenu.ts`). |
| `hero/` | Герой и объекты в нем: компоненты и геометрия (`heroGlobe`, `heroShapes`, `heroScroll`, `heroTypes`). |
| `scenes/` | Живые сцены `AboutScene`, `BoardScene`, `PricingScene` и их хуки `useSceneGaze`, `useSceneProgress`. Сцены полос услуг лежат по одной в `bands/` и грузятся через `next/dynamic` из `BandArt`, общие детали - в `bandParts`. |
| `site/` | Общие для всех страниц элементы из `layout.tsx`: `Footer`, `ClientDecor` и то, что он подгружает (`SiteMotion`, `ScrollLinkedMarks`, `ScrollToTop`). |

Правила:

- **тексты и данные - в `content/`, оформление компонента - в его модуле;**
- **файл лежит в папке фичи, которая его использует.** Хук или вычисления, нужные одной
  папке, лежат рядом с ее компонентами; в `lib/` - только то, что нужно и клиенту, и
  серверу. Внутри папки импорт `./X`, между папками `@/components/папка/X`;
- **направление импортов:** `app` → `components`, `lib`, `content`; `components` →
  `components`, `lib`, `content`; `lib` → `content`; `content` → только `content`.
  Нарушение было: тип знака карточки лежал в компоненте, и данные импортировали из
  `components/` (исправлено 2026-09-11). Второе: движок прыжков по разделам лежал в
  `components/nav`, а зависели от него герои и сцены, то есть папка меню управляла
  анимацией 3D. Вынесен в `components/hub` (2026-09-12) и удален вместе с хабами
  (2026-09-22);
- **одна разметка на обе ширины.** Блок карточек не дублируется на десктопную верстку
  и мобильную карусель: класс `rail` в `globals.css` превращает ту же сетку в
  горизонтальную ленту с примагничиванием, `railcard` дает карточке рамку. Так было в
  пяти местах, и страница цен держала в разметке 20 карточек вместо 10 (2026-09-12);
- **лента - сетка, строки карточек выравнивает subgrid.** Карточка ленты задает
  `--rail-rows` (сколько в ней строк: заголовок, линия, текст, кнопки) и берет строки
  ленты через `grid-template-rows: subgrid`; `height: 100%` у карточек не ставится, он
  отключает растяжение. Подбор `min-height` не держал: высоты карточек расходились на
  20-100px, линия под заголовком гуляла на 11px, заголовки тарифов на 18px (2026-09-13);
- **`content-visibility: auto` у `.section` меняет высоту страницы на ходу.** Правило
  осталось от прыжков по якорям: неотрисованный раздел занимает 720px, и цель,
  посчитанная один раз, промахивалась (перелет до 2325px). Прыжков больше нет, но
  свойство на месте, и оно же делает секцию системой отсчета для `position: fixed` -
  из-за этого окно просмотра работы садилось не в экран (2026-09-13, 2026-09-22);
- **прокрутку посетителя определять по его действиям, а не по сдвигу позиции.**
  Браузер и Next прокручивают страницу сами: плавно к якорю при загрузке и при
  дорисовке разделов. Сравнение позиции принимало это за посетителя, прекращало посадку
  на якорь, и страница уезжала на 1641px. Тем же способом определялось направление
  прокрутки на телефоне, чтобы прятать нижнюю полосу разделов; полоса удалена вместе с
  хабами, атрибут `data-scroll-dir` больше не ставится (2026-09-13, снято 2026-09-22);
- **при открытом меню прокрутку держит только `<html>`.** С `overflow: hidden` еще и на
  `<body>` тот становился контейнером прокрутки, sticky-шапка уезжала, ее переводили в
  `position: fixed`, и страница прыгала вверх на 44px (2026-09-13);
- **`theme-color` не объявлять в `viewport` Next.** Тег один, его ведут скрипт темы в
  `app/layout.tsx` и `ThemeToggle`. С `themeColor` в `viewport` на странице было два
  тега разного цвета, и какой возьмет браузер, не гарантировано (2026-09-13);
- **проверка заявки только в `lib/enquiry.ts`.** Форма и API вызывают одну функцию.
  Пока проверка жила в двух местах, клиент и сервер разошлись дважды: форма отправляла
  направление, которого не было в списке сервера, а позже форма перестала проверять имя;
- **у услуги один идентификатор - ее `slug`.** Реестр `services` в
  `content/services.ts` задает slug, название, группу, тему заявки и метку. По тому же
  slug называются адрес страницы, знак карточки (`CardMark`), фигура героя
  (`HeroShape`), тексты героя, сноска и заголовок страницы в `content/site.ts`.
  `HeroShape` и знак выведены из `ServiceSlug` типом, поэтому лишнее имя не
  собирается. До аудита 2026-09-22 у лендинга было семь имен: `landing` в адресе,
  `websites` в текстах и сноске, `sites` в фигуре, `websites` в знаке, `web` в
  префиксах переменных.
- **название услуги одно на все места.** Его дает `label` реестра: панель меню,
  карточка главной, подвал и страница цен берут его оттуда. Раньше меню звало услугу
  «Маркетплейсом», карточка - «Интернет-магазином», и это видел посетитель.
- **тема заявки - union `Interest`, а не строка.** Тот же список показывает чипами
  форма, а маршрут API принимает только значения оттуда. Пока тема была строкой, ее
  выписывали четырьмя списками плюс литералами на страницах, и совпадение держалось
  вручную. Страница услуги называет свой slug один раз и берет тему через
  `interestOf`.
- **новая услуга** = запись в `services` + папка `app/services/<slug>` + запись в
  `serviceMeta` + тексты героя и сноска в `content/site.ts` + карточка в `paths` и
  тексты в `content/products.ts` + знак в `CardMark` и фигура в `heroShapes`. Забытое
  место показывает компилятор: ключи всех этих таблиц проверяются по `ServiceSlug`.
- **страница услуги собрана из полос, а не из разметки.** `ClaimRow`, `SceneBand`,
  `PlanBand`, `CompareBand` в `components/blocks` держат раскладку, отступы лежат в
  `ServiceBand.module.css`. Девять страниц повторяли два шаблона инлайновыми стилями:
  47 вхождений `style={{ }}` в `app/services` со значениями слово в слово.

## Решения по стеку

| Решение | Почему |
|---|---|
| Next.js 16, App Router, TypeScript | Все страницы пререндерятся в статику - это маркетинговый сайт, его должны находить. Плюс серверный маршрут для формы без второго сервиса и `next/image` для фотографий. |
| Глобальный CSS + CSS-модули | Блок `<style>` из прототипа объявлен источником истины, поэтому его словарь классов перенесен в `app/globals.css` почти дословно. Частное оформление компонентов разложено по `*.module.css`. |
| Тексты типизированными модулями | Каждый экран - это map по массиву из `content/`. Переход на CMS позже = замена модуля, а не страниц. |
| Шрифты через `next/font` | Самохостятся при сборке, к Google из браузера посетителя обращений нет. |

## Шрифты

**Заголовки - Unbounded, текст - Manrope.** Обе гарнитуры подключены через `next/font`
с сабсетами `latin` и `cyrillic`.

Universal Sans Display **снят**. У него нет ни одного кириллического глифа (0 из 66,
диапазон U+0400-04FF пуст), и это была лицензия personal-use, которую нельзя
публиковать. Файл вынесен из `public/` в `../_superseded/fonts/`, чтобы он не уезжал на
сервер. Сменить дисплейную гарнитуру - одна строка `--font-display` в `app/globals.css`.

## Отступления от дизайн-системы Showroom

Приняты заказчиком 2026-09-11. Правила Showroom («ноль теней», «ноль градиентов»,
«единственная анимация - смена цвета», «иконок нет») в этом проекте действуют с
исключениями ниже. Сверяйтесь с этим списком, а не с readme дизайн-системы:

- темная тема: переключатель `ThemeToggle`, токены в `html[data-theme='dark']` в
  `globals.css`;
- живые сцены `AboutScene`, `BoardScene`, `BandArt`, `CardMark` и объекты в героях;
- появление блоков при прокрутке (`SiteMotion`) и анимация входа страниц
  (`app/template.tsx`);
- тени и градиенты сверх двух исходно разрешенных (фон героя и подсветка курсора);
  подсветка курсора работает также на шапке и сценах;
- вес 600 и радиусы 2 и 8 px в отдельных местах;
- собственные SVG-иконки: тема, «наверх», навигация по разделам;
- метка «ХИТ!» у VPN / AI, ее текст в `content/nav.ts` (`hitBadge`).

Все анимации отключаются при `prefers-reduced-motion`.

## Что перенесено из прототипа с изменениями

- **Контейнерные запросы → медиазапросы** на тех же ширинах (1120 - шапка
  сворачивается, 1024 - `.g4` и `.gcards` в два столбца, 768 - все в один). Единственный
  контейнерный запрос оставлен на `.mediacard`: название на карточке масштабируется от
  карточки, а не от страницы.
- **`<div onClick>` → настоящие `<button>` / `<a>` / `<input>`.**
- **Форма получила ловушку фокуса, `Esc`, блокировку прокрутки и возврат фокуса.**
  При переключении на подтверждение фокус уходит на «Готово», а сам блок объявляется
  через `role="status"`.
- **Выпадающие меню шапки и навигация по разделам работают с клавиатуры**: открываются
  по фокусу, строки - ссылки, `Esc` закрывает и возвращает фокус на кнопку. По
  наведению закрытие отложено на 200 мс и отменяется возвратом курсора. Поведение
  общее, в `components/nav/useHoverMenu.ts`.
- **`hero-object.js` стал React-компонентом** (`HeroObject.tsx`), а не custom element,
  чтобы ничего не регистрировалось во время SSR. Грузится лениво. Исходник прототипа
  лежит в `../_superseded/hero-object.js`.
- **Таблицы сравнения остались сеткой `.divrow`**, а не `<table>`: под 900px строка
  превращается в карточку ленты, название столбца печатается в самой ячейке. Разметка
  при этом одна, семантику несут ARIA-роли.
- **Добавлена страница 404** - в дизайне ее не рисовали.

## Персональные данные (152-ФЗ)

Форма собирает имя, почту или телефон и направления обращения, поэтому обработка идет
по 152-ФЗ. Сделано:

- **Страница `/privacy`** - политика обработки ПДн, текст в `content/legal.ts`, ссылка в
  футере и у галочки согласия.
- **Галочка согласия в форме** (`components/contact/Consent.tsx`). Не отмечена заранее:
  предзаполненная галочка согласием не считается. Без нее форма не отправляется, а
  сообщение об этом - предложение, а не красная рамка.
- **Проверка на сервере.** `app/api/enquiry` вызывает ту же `checkEnquiry` из
  `lib/enquiry.ts`, что и форма, и отклоняет заявку без `consent: true` со статусом 400:
  клиентскую проверку можно обойти. Нативная валидация браузера выключена
  (`noValidate`); атрибуты `required` оставлены для скринридера.
- **Доказательство согласия.** Вместе с заявкой уходят `consent`, `policyVersion` и
  `consentAt`. При содержательной правке текста политики поднимайте `policyVersion` в
  `content/legal.ts`.

Что еще нужно сделать человеку, а не коду:

1. **Отдать текст политики юристу.** Это заготовка по структуре 152-ФЗ, а не
   проверенный документ.
2. **Заменить реквизиты оператора** и названия хостинга и CRM в `content/legal.ts`:
   сейчас там квадратные скобки, и они видны на странице.
3. **Дополнить разделы 3 и 6 политики**, если появятся Яндекс.Метрика, чат или пиксель:
   сейчас там написано, что аналитики и cookie нет.
4. **Уведомление в Роскомнадзор** о намерении обрабатывать ПДн подает оператор.

## Что заглушено и где

| Заглушка | Файл | Что нужно до запуска |
|---|---|---|
| Отправка заявки | `app/api/enquiry/route.ts` | Заявка уходит письмом через SMTP Яндекс 360 (`nodemailer`). На хостинге задать `SMTP_USER`, `SMTP_PASS` (пароль приложения) и при необходимости `ENQUIRY_TO`, образец - `.env.example`. Без них в разработке заявка пишется в консоль, в проде маршрут отвечает 503, а не проглатывает заявку. От спама: скрытое поле-ловушка `website` и не больше 5 заявок с адреса за 10 минут (счетчик в памяти процесса). |
| Тексты разделов | `content/products.ts`, `content/site.ts` | Тексты хабов переписаны и дополнены подрядчиком в 0f3388d. Перед запуском вычитать с заказчиком. |
| Коммерческие условия | `content/pricing.ts` | Цены «от» проставлены по списку заказчика 2026-09-13, у «Почта и уведомления» 2026-09-14. |
| Фото | `content/team.ts` | Подключены портреты девяти человек: двух основателей и семи специалистов (2026-09-14, файлы `public/photos/founder-*.webp` и `team-*.webp`, 800px по ширине). Все идут одним списком `team` и одной лентой на странице. Путь к файлу - поле `src` у слота (`ImageSlot`), слот без `src` рисует рамку-заглушку с подписью. |
| Анимация героя | `content/settings.ts` | `cursorHighlight` и `heroObject` включены; итоговое значение - открытый вопрос, правится одной строкой. |
| Редиректы | `next.config.ts` | 307, пока структура может меняться. Перед запуском `permanent: true`. |
| `noindex` | `app/layout.tsx` | `robots: { index: false }`. Снять перед запуском. |

## Чего в дизайне нет вообще

Это не пробелы сборки: этого никто не рисовал, а для русского рынка часть обязательна.

- Реквизиты юрлица в футере, пользовательское соглашение, уведомление о cookie.
- Логотипа нет: начертание `19×17` - это набор с разрядкой `.4em`.
- Нет OG-картинок и sitemap. Иконка вкладки `app/icon.svg` (19×17 одной строкой на синем)
  нарисована по присланному образцу, `app/apple-icon.png` 180px снят с нее же для iPhone.

## Что проверено

Страница «О нас»: одна лента на всю команду, 2026-09-22. По слову заказчика
отдельной полосы с портретами основателей быть не должно - они едут той же лентой,
что и специалисты, первыми ее карточками.

- Списки `founders` и `developers` в `content/team.ts` слиты в один `team` из девяти
  человек, тип `Founder` переименован в `Person`. Заголовков стало один вместо двух:
  «Команда», текст объединен.
- Удалены разметка полосы основателей в `app/about/page.tsx` и файл
  `app/about/about.module.css` целиком: единственный класс `.founders` держал сетку
  этой полосы.
- Замеры в headless Chrome: на 1440 лента отдает девять человек в порядке
  Чебыкин, Шульга, дальше специалисты, карточка 294x493; на 390 карточка 150x394.
  Горизонтальной прокрутки и ошибок в консоли нет. В ленте 27 карточек - это три
  копии списка, как и было: на них держится бесконечная прокрутка.

Правки по итогам аудита, 2026-09-22. Сборка (19 маршрутов), `tsc` и `eslint`
чистые после каждой.

- Реестр услуг (`content/services.ts`). Семь имен одной услуги сведены к одному slug,
  `HeroShape` и знак карточки выведены из `ServiceSlug` типом. Тема заявки стала union
  `Interest`: четыре независимых списка и 11 литералов на страницах заменены реестром,
  чипы формы строятся из него же, `ChoiceRow` стал обобщенным по типу вариантов.
- Названия услуг. Меню, карточки главной и страница цен звали одну услугу тремя
  наборами названий; взяты развернутые с карточек. Замеры в headless Chrome: панель
  960px в окне 1440 без переносов и горизонтальной прокрутки, девять карточек с
  верными адресами, мобильное меню на 390px строками по 46px.
- Полосы страницы услуги вынесены в `ClaimRow`, `SceneBand`, `PlanBand`, `CompareBand`.
  Инлайновых стилей в `app/services` не осталось (было 47), страницы стали короче
  вдвое: 275 строк против 494. Тарифные ряды приведены к одной форме `Plan`.
- Сцены полос разнесены по файлам и чанкам. Было 851 строк в одном файле и 25 КБ без
  сжатия на каждую из шести страниц; стало общий чанк 9.5 КБ плюс своя сцена 6.3-7 КБ.
  Отрисовка на сервере сохранена, HTML страницы не изменился в размере. Проверено на
  dev-сервере: в svg 68-100 элементов, из них 25-37 с запущенной анимацией.
- Редакция политики в заявке пишется сервером. Поле принималось из тела запроса, и
  доказательство согласия по 152-ФЗ подделывалось подменой строки. Если страница
  назвала другую редакцию, это идет в письмо отдельной строкой. Повторы в списке тем
  схлопываются, длина ограничена восемью.
- Концы строк: добавлен `.gitattributes` с политикой LF. Индекс уже был LF, поэтому
  `git add --renormalize` не изменил ни одного файла.
- Комментарии переведены на русский, английских в проекте не осталось (было 29 файлов).
- Проверка маршрута заявки настоящими запросами: заявка с подделанной редакцией
  проходит и уходит с серверной; пятьдесят повторов одной темы схлопываются; девять тем
  с одной чужой, пустой список и отсутствие согласия отклоняются с разными текстами;
  ловушка для ботов отвечает 200 без письма. Отправка письма на dev-сервере отвечает
  502: в `.env.local` задан `SMTP_USER`, но SMTP не принимает - это настройка
  окружения, не код.
- Обход девяти страниц услуг на 1440: состав полос, сцены, тарифные ряды, таблицы
  сравнения и сноски на местах, ошибок в консоли и горизонтальной прокрутки нет.

Сплошной аудит проекта, 2026-09-22. Сборка (19 маршрутов), `tsc` и `eslint` чистые.
Считано по всему дереву, не выборкой.

- Мертвых модулей нет: из 140 отслеживаемых файлов без входящих импортов только 17
  маршрутов `app/`, и это точки входа. Неиспользуемых классов в CSS-модулях 0, в
  `globals.css` 0, неиспользуемых файлов в `public/` 0, объявленных и никем не читаемых
  токенов в `globals.css` 0.
- Найдено и удалено: `heroDockProgress` и `heroApproachProgress` в `heroScroll.ts`
  (остались от прыжков по разделам, удаленных вместе с хабами), проп `id` у `Hero`
  вместе с комментарием про несуществующее меню Sites, комментарий в `content/site.ts`
  про удаленный `components/nav/hubNav`, строка README про удаленный `drawerLinks`.
  Экспорт снят у `hitBadge`, `serviceGroupTitles`, `serviceLinks`: их читает только
  сам `content/nav.ts`.
- Направление импортов соблюдено: `content` импортирует только `content`, `lib` только
  `content`, обращений из `components` в `app` нет, обратных импортов между папками
  `components` нет. Ниже всех лежит `ui` (тянет только `content`); `contact` по факту
  такой же нижний слой, его тянут четыре папки из пяти.
- Одно и то же направление заявки (`interest`) выписано четырьмя независимыми списками:
  `services` в `content/nav.ts`, `paths` в `content/products.ts`, `pricingBands` в
  `content/pricing.ts`, `interestByShape` в `components/hero/Hero.tsx`, плюс 11
  строковых литералов на страницах услуг. Сейчас все 47 значений совпадают, сверено
  посимвольно, но тип у поля `string`, и расхождение компилятор не поймает. Форма
  принимает только значения из `contactCopy.interests` - разъехавшийся литерал даст
  заявку с чужим направлением или без предвыбора.
- У одной услуги до семи разных имен: лендинг это `landing` в адресе и `serviceMeta`,
  `websites` в `heroes` и `footnotes`, `sites` в `HeroShape`, `websites` в `CardMarkId`,
  префикс `web` у текстов. Магазин: `marketplace`, `store`, `store`, `store`, `store`.
  Обновление: `redesign` везде, кроме `HeroShape`, где `update`. Связь держится
  глазами, а не типом.
- Названия услуг в меню и на карточках главной расходятся: «Маркетплейс» против
  «Интернет-магазин», «Обновление дизайна» против «Обновление сайта», «Интеграция 1С»
  против «Интеграция с 1С», «Интеграция CRM» против «Интеграция с CRM». Это видно
  посетителю; какие названия верные, решает заказчик.
- `serviceMeta` объявлен как `Record<string, ...>`: опечатка в ключе даст `undefined`
  вместо ошибки сборки, страница уйдет без заголовка и описания.
- Девять страниц услуг повторяют два шаблона: шесть собраны из героя, полосы `g4`,
  полосы `g2` со сценой и полосы с `PlanFigure`; две из героя, полосы `g4` и
  `CompareTable`. Раскладка задана инлайновыми стилями, 110 вхождений `style={{`
  по проекту, из них 47 на страницах услуг, и значения там повторяются дословно.
- Шесть сцен `BandArt` лежат в одном клиентском чанке: 25 КБ без сжатия на страницу,
  которая рисует одну сцену. Замер на собранной копии: `2680-*.js` содержит все шесть
  имен сцен, страницы `landing` и `support` сцен не используют и этот чанк не получают.
- `policyVersion` для письма берется из тела запроса, если клиент прислал строку
  (`app/api/enquiry/route.ts`). Доказательство согласия по 152-ФЗ подделывается
  подменой поля; серверная константа используется только как запасная.
- Поле `interests` в заявке фильтруется по списку разрешенных, но длина массива не
  ограничена: повторы одного и того же направления пройдут проверку и попадут в письмо.
- `.gitattributes` в репозитории нет. Индекс везде LF, рабочая копия смешана: 64 файла
  CRLF, 56 LF, `tsconfig.json` с обоими видами внутри. На своей машине незаметно, на
  второй даст diff по всему файлу.
- Комментарии написаны на двух языках: английские остались в 20 файлах, больше всего в
  `heroShapes.ts`, `settings.ts`, `HeroObject.tsx`, `layout.tsx`, `Overlay.tsx`.
- Проп `secondaryHref` у `Hero` не передается ни с одной страницы, везде работает
  значение по умолчанию `/pricing`. Оставлен: это точка настройки, а не остаток.
- К запуску: `robots: { index: false }` в `app/layout.tsx`, редиректы `permanent: false`
  в `next.config.ts`, отсутствие `metadataBase`, `openGraph`, `app/sitemap.ts`,
  `app/robots.ts` и картинки для ссылки в мессенджере.

Письмо-презентация, 2026-09-21. Шаблон в `emails/presentation.html`, картинки в
`public/mail/`, порядок работы с ними - `emails/README.md`. Сайта письмо не
касается: Next его не собирает, общий у них только домен, с которого раздаются
две картинки.

- Снято в headless Chrome: на 700px письмо 600×2479, на 375px перестраивается в
  375×2975 без горизонтальной прокрутки, обе картинки грузятся. Размер файла
  17 КБ при пороге обрезки Gmail в 102 КБ.
- Резиновая ширина держится на `max-width` у таблицы и картинки. Сперва сфера
  стояла с `width:600px` в стиле, и на телефоне письмо не сжималось: 624px и
  обрезанный правый край. Ширину картинки заменили на `width:100%`.
- Цена не разрывается между «от» и суммой: у каждой суммы `white-space:nowrap`.
  Без него «Почта и уведомления от 10 000 ₽» переносилось так, что сумма уходила
  на отдельную строку.
- Фон плашки со сферой ровный `#eef3fb`, тем же цветом залита ячейка. Вариант с
  градиентом дал видимую полосу внизу после сжатия PNG в палитру.

Раздел «Работы» и заметность 3D-моделей, 2026-09-20. Сборка (11 маршрутов), `tsc` и
`eslint` чистые, замеры на собранной копии в headless Chrome.

- Модели заметнее. Средняя непрозрачность закрашенного пикселя холста, боевой сайт
  против собранной копии: глобус главной 21.8 -> 33.0, курсор лендинга 21.8 -> 34.1,
  магазин 24.2 -> 37.5, VPN 22.4 -> 36.1; на телефоне 390px курсор 21.3 -> 32.8,
  магазин 21.6 -> 35.9. Рамки моделей не изменились, правки размеров от 2026-09-14
  сохранены.
- Первый заход поднял только ПК: на телефоне модель рисуется без точек и всего по
  четырем корзинам прозрачности, а потолок корзин стоял выше максимума формулы (0.3
  против 0.22), поэтому верхние корзины не доставались. Потолок приравнен к максимуму,
  в lite-режиме ребрам дана своя доля (0.3 против 0.22).
- `/works`: 17 карточек, по одной на файл макета; связь держит поле `source`. ПК
  1440px - три в ряд по 393×491, 1024px - две по 450×528, телефон 390px - одна
  350×504. Горизонтальной прокрутки нет ни на одной ширине.
- Своя фигура героя `works`: кольцо с галочкой внутри, ПК 495×493, телефон 390px
  345×344. Кольцо повторяет круг модели CRM (центр 0.5, 0.5, радиус 0.44), поправки
  в `SHAPE_FIT` у обеих фигур нет, поэтому размер задается общим вписыванием.
  По высоте фигура встает вровень с соседними моделями: у всех около 492px на ПК.
  До нее заказчик отклонил рамку экрана с плитками, карандаш, циркуль и лупу
  (2026-09-20). Скругления у фигур этого вида не ставить: на них трубка набирает
  кольца часто и в углу получается веер из линий. Ветки в `applyHeroScroll` у
  фигуры нет, при прокрутке она неподвижна.
- Нажатие на карточку открывает макет целиком в оверлее (`WorksGallery`,
  `Overlay size="wide"`): название закреплено сверху, закрытие по крестику, `Esc` и
  клику мимо, прокрутка страницы блокируется. Проверено настоящим кликом мыши
  (CDP `Input.dispatchMouseEvent`) на ПК 1440px и телефоне 390px: кнопка накрывает
  карточку целиком (391×489 и 348×502) и лежит поверх снимка.
- Окно просмотра уходит в `<body>` порталом (`Overlay`). Без портала панель
  оказывалась внутри `.section`, а у секции стоит `content-visibility: auto` - это
  включает `contain: paint`, и секция становится системой отсчета для
  `position: fixed`. Нижние карточки открывались тогда не в экране, а в начале
  секции: замер на 17-й карточке давал рамку подложки `y = -3180` при высоте 4975
  вместо экрана. После портала подложка совпадает с экраном (1440×900 и 390×844),
  прокрутка страницы не двигается: 3420 до нажатия и 3420 после, на телефоне 8495
  и 8495.
- У широкой панели `overflow` не переопределяется: от `.panel` приходит
  `overflow-y: auto`. Со старым `overflow: hidden` макет обрезался по высоте экрана
  без возможности пролистать - панель 852px при содержимом 1630px.
- Отдельное наблюдение, к окну просмотра отношения не имеющее: нажатие на «Обсудить задачу» в шапке
  смещает страницу на 450px вверх плавной прокруткой. Воспроизводится и на версии
  до портала, то есть дело не в окне; источник не найден, заказчику сказано.
- Кнопка карточки задается селектором `.card.clickable .hit`. Одного класса мало:
  глобальное правило `[data-cursor-glow] :is(.btn, button, ...)` в `app/globals.css`
  ставит кнопкам `position: relative`, и `absolute` с `inset: 0` не доживал до
  экрана - кнопка схлопывалась в нулевую высоту, мышью по карточке было не попасть.
  Проверка через `element.click()` этого не ловит: она не зависит от геометрии.
- Снимки в `public/works` - выгрузка заказчика от 2026-09-22 (см. блок ниже).
- Пункт «Работы» стоит в шапке (шире 1024px), в мобильном меню и в группе «Компания»
  в футере.

Подпись «Смотреть макет» убрана, 2026-09-22. По слову заказчика: плашка появлялась
при наведении на карточку работы и не нужна. Удалены класс `.peek` в
`WorkCard.module.css` и его разметка. Нажатие на карточку по-прежнему открывает макет
целиком, проверено настоящим кликом. Такая же плашка у холста «Как мы ведем проект»
снята раньше в тот же день.

Панель услуг переехала под «Главную», 2026-09-22. Сборка, `tsc` и `eslint` чистые.

- Заказчик: отдельной вкладки «Услуги» в шапке быть не должно, услуги раскрываются при
  наведении на «Главную». Вкладок стало четыре: Главная, Работы, Цены, О нас.
- `ServicesMenu` переименован в `HomeMenu`. Клик по вкладке ведет на `/`, прокрутка к
  блоку `#services` убрана вместе со вкладкой «Услуги»; сам якорь на секции остался,
  на него ведут редиректы `/integrations`, `/maintenance`, `/services`.
- У названия вкладки стрелка, при наведении разворачивается. Без нее панель под
  «Главной» не угадывается: этот пункт нигде не принято раскрывать.
- Подсветка: на странице услуги активна «Главная». Мобильный ящик не менялся, там
  услуги и так перечислены построчно.
- Замеры: вкладок 4, панель 960px помещается в окно, девять строк с верными адресами,
  переход по строке открывает услугу, клик по вкладке ведет на главную, ошибок в
  консоли нет.

Витрина убрана, список услуг - на главной, 2026-09-22. Сборка, `tsc` и `eslint` чистые.

- Заказчик: витрина `/services` повторяет блок карточек на главной, отдельная страница
  не нужна. Витрина, `ServiceGrid` и hero витрины удалены.
- Карточек на главной стало девять вместо восьми: «Создание сайта» разделена на
  «Лендинг» и «Многостраничный сайт», теперь на главной видны все услуги. Сперва знак у
  этих двух был общий, и в ряду они читались как одна услуга; многостраничному нарисован
  свой - стопка из трех листов, строка разделов с переезжающей подсветкой и блоки
  страницы, которые гаснут на пересменке. У лендинга остался экран браузера с адресной
  строкой.
- Вкладка «Услуги» ведет на `/#services`. С другой страницы к якорю прокручивает
  браузер, на самой главной - обработчик в `ServicesMenu`: Next не прокручивает к
  якорю, когда страница не меняется. Замеры: с главной 0 -> 844, блок у верха окна;
  с `/works` переход на `/#services`, прокрутка 847.
- Из мобильного ящика убрана строка «Услуги»: она вела бы на блок главной, который в
  том же ящике расписан построчно.
- `/services` оставлен редиректом на `/#services`: адрес прожил день, мог попасть в
  закладки.
- Проверка: 9 карточек с верными адресами, `/websites#crm` открывает `/services/crm`,
  `/vpn-ai#ai` - `/services/ai`, ошибок в консоли нет.

Услуги отдельными страницами, 2026-09-22. Сборка, `tsc` и `eslint` чистые.

- Заказчик: «две вкладки на девять услуг, и VPN с локальным AI свалены на одну
  страницу». Сделана одна вкладка «Услуги» и девять отдельных страниц
  `/services/<slug>` плюс витрина `/services`.
- Список услуг теперь один, `services` в `content/nav.ts`. Из него собираются панель в
  шапке, мобильное меню, подвал, витрина и предвыбор темы заявки. До этого девять услуг
  были выписаны в пяти местах: `nav`, `siteServices`, `infraMenuGroups`,
  `interestBySection`, `paths`.
- У каждой услуги свой `title` и `description` (`serviceMeta` в `content/site.ts`).
  Раньше семь услуг делили одно описание страницы `/websites`.
- Старые ссылки с якорем: `/websites#crm` приходит на сервер как `/websites`, якорь
  браузер серверу не отправляет. Редирект ведет на витрину, якорь браузер переносит
  сам, дальше его разбирает `LegacyHashRedirect` и заменяет адрес на страницу услуги.
  Проверено: `/websites#crm` открывает `/services/crm`, `/vpn-ai#ai` - `/services/ai`.
- Удалена навигация по якорям: `HubNav`, `HubRail` (378 строк), `HubPager` (194),
  `useHubSectionHash`, `hubLinks`, `hubScroll` (337), `SitesMenu`, `ScrollToHash`,
  `HashLink`. Вместе с ней из `HeroObject` убрана поза прыжка (`beginJumpPose`,
  `jumpTween`, взгляд вслед прыжку), из `useSceneGaze` - отложенный взгляд, из
  `SiteHeader` - расчет направления прокрутки для нижней полосы. Всего минус 1600 строк.
- Блок «Другие услуги» внизу страницы услуги сделан и тут же убран по слову заказчика:
  переход между услугами дают панель в шапке, подвал и витрина, четвертого списка тех же
  девяти ссылок на странице не нужно.
- Панель меню 960px, четыре колонки. На 880px «Корпоративный VPN» переносился на две
  строки, метка ХИТ уезжала под название; ширина увеличена, строка не переносится.
- Плитки витрины - `auto-fill`, а не `auto-fit`: с `auto-fit` группа из двух услуг
  растягивалась на всю ширину и плитки между группами были разного размера.
- Сверка ссылок: со всех 15 страниц собрано 15 разных внутренних адресов, все отвечают
  200, битых нет. Все 11 старых адресов отвечают 307 на новые.
- Замеры: 10 маршрутов на ПК 1440 и телефоне 390, 3D-объект рисуется на каждом,
  горизонтальной прокрутки нет, ошибок в консоли нет. Панель помещается в окно на
  1121, 1280 и 1920.

Блок «Как мы ведем проект», 2026-09-22. Сборка (11 маршрутов), `tsc` и `eslint`
чистые.

- Заказчик прислал листы этапов из файла Макета 1 архивом: семь листов плюс сам
  лендинг. Готовой картинки со схемой у него нет, поэтому холст собран скриптом
  (`макеты-исходники/board.mjs`) в один файл
  `public/works/flow/board.webp`, 2404×2382, 451 КБ.
- Сперва блок был сделан восемью отдельными плитками с подписями. Заказчик
  отклонил: нужен один лист, как в Figma. Плитки и файлы `s1..s7` удалены,
  остался один холст, `ProjectFlow` показывает его целиком.
- Раскладка холста повторяет доску в Figma: колонка 620px с этапами 1-3, колонка
  740px с этапами 4, 5, 7, колонка 420px с этапом 6 и готовой страницей под ним.
  Фон `#e4e5e6`, листы ложатся на него белыми прямоугольниками без рамок,
  подписи набраны Arial через SVG.
- Листы разного формата - от полосы 5190×1484 с палитрой до подборки 5546×8836 с
  фотографиями, - поэтому вписаны по ширине колонки целиком, без обрезки.
- Нумерация подписей идет по именам файлов. Внутри самих листов она сбита: на
  третьем стоит «Этап 2», на четвертом «Этап 3». На холсте номера свои, внутри
  листов видны прежние.
- Нажатия у холста нет. Сперва он открывался тем же `Overlay size="wide"`, что
  и макеты работ, но заказчик попросил убрать: лист показан целиком, открывать
  нечего. Вместе с окном убраны кнопка-накладка, подпись «Смотреть целиком» и
  `data-cursor-glow`, чтобы холст не выглядел нажимаемым. `ProjectFlow` после
  этого стал серверным компонентом - состояния в нем не осталось.
- На телефоне холст ужимается до 348px по ширине, детали внутри листов не
  читаются: это цена одного общего листа вместо восьми плиток.
- Исходные листы (89 МБ) лежат в `1917_site/макеты-исходники/этапы-макет-1`.
- Замеры: холст 1038×1028 на ПК и 348×345 на телефоне, горизонтальной прокрутки
  нет.
- `app/works/works.module.css` удален: в нем оставался только класс `.plan` от
  прежней рамки-заглушки.

Снимки работ из макетов, 2026-09-22. Сборка (11 маршрутов), `tsc` и `eslint`
чистые. На странице 14 карточек.

- Первые три - собственные работы заказчика (WARD group, грузоперевозки,
  GreenSpace), они стоят в начале списка по его просьбе. Исходники `my1-my3.png`
  шириной 916-1010px, то есть уже панели просмотра (1040px). Апскейла не делаем:
  полный снимок сохраняет свою ширину, в окне он немного мягче макетов Figma.
- Заказчик выгрузил из Figma 11 макетов из 17 - те, что хочет показывать. PNG,
  ширина 3840px (2x от макета 1920). Остальные шесть карточек (макеты 2, 4, 5, 7,
  8, 10) сняты со страницы: заказчик добавит вместо них другие работы.
- Формат выбран PNG: без потерь, дальше все равно пережимается в webp, и двойного
  сжатия по тексту и плоским заливкам не возникает. Выгрузка 4x отклонена по двум
  причинам: webp не кодирует сторону длиннее 16383px, а самый длинный макет при 4x
  дал бы 19800px; и сайт не отдает картинки шире 1600px (`deviceSizes` в
  `next.config.ts`).
- Из каждого PNG собираются два файла: `<id>.webp` - кадр 3:2 от верха макета,
  ширина 1200px, для карточки; `<id>-full.webp` - весь лендинг шириной 1600px, для
  окна просмотра. Итого 22 файла, 3.7 МБ. Исходные PNG (132 МБ) в репозиторий не
  берутся, лежат в `1917_site/макеты-исходники`.
- У карточек добавлены превью в `content/photoBlur.ts`: карточка работы рисуется
  тем же `Photo`, и без превью на ее месте висела серая рамка.
- Названия и описания переписаны по самим макетам, а не по мелким превью из `.fig`.
  Прежние были неверны: Макет 14 назывался «Ресторан», а это лендинг обжарщика кофе
  с каталогом сортов; Макет 6 - «Меню заведения», а это HealthBowl со смузи-боулами;
  Макет 15 стоял как многостраничный сайт, а это лендинг доставки суши SUSHIBU.
  Идентификаторы приведены к содержимому: `menu-dark` -> `healthbowl`, `restaurant`
  -> `roastery`, `delivery` -> `foody`, `sushi-site` -> `sushi`.
- После замены файлов страница показывала старые снимки: в кеше оптимизатора
  (`.next/dev/cache/images`) остались варианты AVIF, собранные со старых файлов с
  теми же именами, а `minimumCacheTTL` стоит 30 дней. Проверка: один и тот же адрес
  отдавал 640×427 в webp (MISS) и 319×213 в avif (HIT). Лечится удалением
  `.next/dev/cache/images`, файлы пересобираются сами. На проде не возникает: там
  кеш пустой после выкладки.
- Плашка типа работы (`kind`) проставлена по шапке макета: корзина рядом с меню
  и каталог с ценами - интернет-магазин (GreenSpace, CoffeSUN, Stake House,
  FOODY); пункты «О компании», «Наши работы», «Контакты» вместо якорей одной
  страницы - многостраничный сайт (грузоперевозки); остальные девять - лендинг.
  Признак косвенный: по одному экрану остальные страницы не видны, типы
  подтверждает заказчик.
- Замеры после правки: карточек 14, на ПК 1440px три в ряд по 393px, на телефоне
  390px одна по 350px, горизонтальной прокрутки нет. В карточку приходит вариант
  420×280 на ПК и 358×239 на телефоне, в окно просмотра - 1040×2032.

Долгая загрузка фото команды, 2026-09-15. Сборка, `tsc` и `eslint` чистые.

- На 1917.online, 1440px: фото команды запрашивались только после прокрутки до
  карусели, седьмое - после нажатия стрелки, вариант 1600px для карточки 294px. Первый
  байт 0.8 с даже при попадании в кэш оптимизатора, без кэша 1.9 с: задержка сети и
  сервера, в коде ее не убрать.
- На собранной копии: все 7 фото запрашиваются при загрузке страницы, вариант 640px
  (на ПК и на телефоне 390px), стрелки новых запросов не дают. Пока фото грузится,
  в рамке размытое превью из `content/photoBlur.ts`: оно есть у 23 картинок в HTML.
  HTML страницы 101 КБ (превью встроены в разметку).

Правки заказчика, 2026-09-14. Сборка, `tsc` и `eslint` чистые, замеры на собранной копии в
headless Chrome.

- 3D-герои на телефоне 390×844 (поправка `narrowScale` в `SHAPE_FIT`, уже 768px): курсор
  лендинга 247×393 (было около 309×491), многостраничный сайт 149×276 (было около
  99×184: рамка считается по разложенным страницам, а в покое они в стопке), магазин
  310×354 (было 345×394), VPN 293×350 (было 345×411). Размеры на ПК не меняются.
- `/about`: фото основателей на планшете 768px - две колонки 342×427 (было одна
  колонка 707×883), на телефоне 350×438 без изменений. Карусель команды на ПК 1440px -
  четыре человека в ряд, фото 294×367 (было три, 402×503). Горизонтальной прокрутки нет.

Правки заказчика, 2026-09-13. Сборка (`next build --webpack`, 10 маршрутов), `tsc` и
`eslint .` чистые. Поведение проверено на dev-сервере в headless Chrome, кадры рисуются.

- Прыжок по рельсу со свежей загрузки `/websites#landing`, 1440×900: 8 из 8 точно в
  начало раздела (до правки дальние разделы мимо, CRM в футере); на `/vpn-ai` точно.
- Переход по ссылкам футера на раздел другой страницы, 390px: 53 из 53 (было 36
  промахов). Полная загрузка `/websites#onec` на 390 и 1440px: раздел под шапкой.
- Переход между страницами через мобильное меню после прокрутки: 20 из 20 открываются
  сверху; на 1917.online так же.
- Ленты карточек на 360, 390 и 430px, все ленты четырех страниц: разброс высоты
  карточек, заголовка и линии 0, вертикальный ход ленты 0 (был 18px).
- 3D-герои: после новой отрисовки кадры 20 героев совпадают побайтно. После поворота
  сечения трубки на ПК 10 из 10 совпадают, скважина VPN на телефоне по центру (смещение
  0, было 0.0125). Модель обновления сайта вращается и на телефоне (было 0 кадров с
  движением из 30).
- Шапка на телефоне (третья правка): не прячется, при открытии меню страница не
  сдвигается в начале, в середине и в глубине страницы (было 44px), шапка на месте.
  Нижняя полоса: ход вверх 190px прячет, вниз 285px возвращает, встречный ход 10 и
  15px не переключает, у верха видна. Свайп по полосе: 60px - на один раздел, 270px -
  на два, короткий бросок 120px - на два (было всегда на один).
- Ленты (третья правка): заголовки в «Три формата работы», парах, тарифах и карточках
  цен стоят по центру своей строки; снимки всех лент четырех страниц на 375px.
- Модель обновления сайта, вторая правка: шестеренка неподвижна (разница кадров в
  центре 0.03-0.26 из 255), стрелки двигаются в каждом кадре и от прокрутки, оборот 18 с.
- `theme-color`: один тег, цвет темы после загрузки, перехода и переключения.
- Подсветка курсора включается от мыши и не включается от пальца.

Не проверено на устройствах: перезагрузка вкладки на iPhone (нужен настоящий Safari;
по коду причина - нативная память Path2D на каждом кадре, убрана) и открытие сайта на
iPhone без VPN (в коде внешних запросов нет, дело в сети или хостинге).

Ревью коммита 10ee617, 2026-09-12. Сборка, `tsc` и `eslint` чистые.

- **Сборка не проходила**: 20 ошибок типов в `PricingMark` и чисто глобальный селектор
  в CSS-модуле навигации. Turbopack в разработке это пропускает, обычная сборка
  отклоняет, поэтому в `npm run dev` дефекты были не видны.
- **Двойной рендер убран в пяти местах**: в разметке страницы цен 10 карточек вместо
  20, на главной 8 вместо 16. Удалены `LoopCarousel` (170 строк ручного
  перетаскивания), `CompareCards`, `PlanFigureGrid` и класс `desk-only`.
- **Навигация разделена**: движок прыжков вынесен из `nav` в `hub`, файл на 532 строки
  разделен на рельс и нижнюю полосу.
- **Дефект мобильной навигации**: нажатие на раздел в нижней полосе не работало,
  область просмотра захватывала указатель, и браузер переадресовывал клик на нее.
  Поймано сравнением настоящего нажатия мышью с программным вызовом кнопки.
- **Переход на раздел другой страницы** шел полной перезагрузкой страницы.
- **Мертвый код**: 680 строк в `heroShapes.ts` - сборка букв из глифов, оставшаяся от
  прежних героев; ветка в `heroScroll`; правила `.wrench`, `.ticket`, `.tick`; параметр
  `uid` в шести сценах; `hubJumpHint`. Найдено линтером и повторным поиском по экспортам
  и классам, итог - 0 находок.
- **Повторы стилей**: карточка ленты была скопирована в три модуля, восемь правил марок
  в два; сведены в `railcard` и `markBase.module.css`. Общие анимации марок вынести
  нельзя: сборщик переименовывает имя анимации внутри модуля, и ссылка на блок в другом
  файле перестает совпадать. Проверено сборкой: анимации молча выключались.
- **Проверено на собранной копии в headless Chrome**, 390 и 1280: размеры блоков и
  моделей до и после совпадают, тема переключается и запоминается, выпадающие меню
  открываются, рельс прыгает по разделам без перезагрузки, полоса листается свайпом и
  открывает раздел нажатием, карусель команды листается стрелкой, мобильное меню
  открывается и закрывается переходом, ошибок в консоли нет.

Ревью коммита 0f3388d, 2026-09-11. Сборка и `tsc` чистые, пререндерятся 8 маршрутов.

- 9 старых адресов отдают 307 точно на свои разделы; браузер переносит якорь
  исходного адреса (`/integrations#crm` → `/websites#crm`).
- Форма: 7 сценариев - по одному на каждую ошибку в порядке полей и успешная отправка.
- API в обход формы: без согласия, с `consent: false`, с направлением не из списка, с
  пустым именем, без почты и телефона, битый JSON - 400 с текстом-предложением;
  валидная заявка - 200.
- Все 38 предустановок направления заявки совпадают со списком чипов формы.
- Фото основателей отдаются оптимизатором картинок.
- Темная тема: 0 нарушений контраста текста на `/about` и `/websites`.
- Навигация: прыжок по пункту меню и по ссылке с другой страницы попадает в свой
  раздел; меню открываются, закрываются с задержкой, реагируют на `Esc` и потерю
  фокуса, а уход фокуса внутрь панели меню не закрывает; мобильное меню совпадает по
  разметке с версией до рефакторинга; функции навигации перенесены в отдельный модуль
  (сейчас `components/hub/hubScroll.ts`) побайтно.
- Типографика: буквы е с точками по всему проекту - 0, длинных тире в комментариях и
  документации - 0;
  в текстах, видимых посетителю, тире оставлены по решению заказчика.
- Файловая архитектура: все 91 файл достижимы из маршрутов; неиспользуемых классов в
  CSS-модулях 0 из 276; импортов против направления слоев 0 из 130; экспортов без
  внешних ссылок 0, кроме 7 типов, которые описывают сигнатуры экспортов.
- Перенос по папкам и обновление до Next 16 сверены с production-сборкой до изменений:
  HTML 7 страниц после нормализации хешей классов, статусы, 9 редиректов и 5 ответов
  API. Единственное отличие - атрибут `data-scroll-behavior` у `<html>`, он добавлен
  намеренно. `npm audit`: 0 уязвимостей.
- Next 16 в dev: ошибок в консоли нет; переход по `/integrations/crm` и по
  `/websites#onec` ставит раздел под шапку (окно 1280×800).
- Поведение в headless Chrome (кадры рисуются), 5 страниц, 1280×800: вычисленные
  стили всех элементов, запущенные анимации, холсты героев и появление блоков
  совпадают с коммитом подрядчика 0f3388d. Эта проверка нашла регрессию Next 16:
  Turbopack подключает CSS в порядке импортов, и `globals.css`, импортированный
  после шапки, перекрывал модульные классы. Поэтому `globals.css` - первый импорт в
  `app/layout.tsx`.
- Встроенная панель браузера Claude Code для проверки анимаций не годится: в скрытом
  окне не работают `requestAnimationFrame` и `IntersectionObserver`.

Не проверено вживую: подсветка активного пункта при прокрутке и настоящее наведение
курсором. Окно браузера при проверке было свернуто, `requestAnimationFrame` не
выполнялся, поэтому меню проверены прямыми вызовами обработчиков. Код
`useHubSectionHash`, от которого зависит подсветка, ревью не менял.
